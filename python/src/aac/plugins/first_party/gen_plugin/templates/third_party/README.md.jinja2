# {{plugin.name}}

## Plugin Commands

{% for command in commands %}

### Command: {{command.name}}

{{command.helpText}}

Example shell usage:

```bash
(.env) $ aac {{command.display or command.name}} -h
```

#### Command arguments

- `-h`, `--help`: Shows a help message
{% if command.input is defined %}
{% for command_input in command.input %}
- `{{command_input.name}}` (`{{command_input.python_type}}`): {% if command_input.description is defined %}{{command_input.description}}{% else %}TODO: add description{% endif %}{% if command_input.default is defined %} (default: {{command_input.default}}){% endif %}
{% endfor %}
{% endif %}
{% endfor %}

{% if plugin_definitions | length > 0 %}

## Plugin Extensions and Definitions

{% for def in plugin_definitions %}

### {{def.root_key | title}} - {{def.type_name}}

TODO: Add descriptive information about {{def.type_name}}

```yaml
{{def.yaml}}
```
{% endfor %}
{% endif %}
