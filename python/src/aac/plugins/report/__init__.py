"""The aac-reporting plugin module."""
# NOTE: It is safe to edit this file.
# This file is only initially generated by aac gen-plugin, and it won't be overwritten if the file already exists.

from aac.plugins import hookimpl
from aac.cli.aac_command import AacCommand, AacCommandArgument
from aac.package_resources import get_resource_file_contents
from aac.plugins.report.aac_reporting_impl import report, report_csv


@hookimpl
def get_commands() -> list[AacCommand]:
    """
    Return a list of AacCommands provided by the plugin to register for use.

    This function is automatically generated. Do not edit.

    Returns:
        list of AacCommands
    """
    report_arguments = [
        AacCommandArgument("architecture_file", "The yaml file containing the report model."),
        AacCommandArgument("output_file", "The file to write the generated YAML report."),
    ]
    report_csv_arguments = [
        AacCommandArgument("architecture_file", "The yaml file containing the report model."),
        AacCommandArgument("output_file", "The file to write the generated CSV report."),
    ]

    plugin_commands = [
        AacCommand(
            "report",
            "Generate YAML output from an Arch-as-Code report definition.",
            report,
            report_arguments
        ),
        AacCommand(
            "report-csv",
            "Generate CSV output from an Arch-as-Code report definition.",
            report_csv,
            report_csv_arguments
        ),
    ]

    return plugin_commands


@hookimpl
def get_plugin_aac_definitions() -> str:
    """
    Returns the CommandBehaviorType modeling language extension to the plugin infrastructure.

    Returns:
        string representing yaml extensions and data definitions employed by the plugin
    """
    return get_resource_file_contents(__package__, "report.yaml")