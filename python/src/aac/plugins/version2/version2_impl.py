"""The AaC Version2 plugin implementation module."""
# NOTE: It is safe to edit this file.
# This file is only initially generated by aac gen-plugin, and it won't be overwritten if the file already exists.

from aac.execute.aac_execution_result import ExecutionResult, ExecutionStatus

plugin_name = "Version2"


def before_version2_version(run_version) -> ExecutionResult:
    """Run the Version  command before the version2 command."""

    # TODO: configure and call the version before command using version2 command inputs
    message: str = "The Version version before handling for the version2 command has not been implemented yet."

    return ExecutionResult(plugin_name, "version2", ExecutionStatus.GENERAL_FAILURE, [message])


def version2() -> ExecutionResult:
    """Print the AaC package version."""
    
    # TODO: implement plugin logic here
    message: str = "The version2 for the Version2 plugin has not been implemented yet."

    return ExecutionResult(plugin_name, "version2", ExecutionStatus.GENERAL_FAILURE, [message])


def after_version2_version(run_version) -> ExecutionResult:
    """Run the Version version command after the version2 command."""

    # TODO: configure and call the version after command using version2 command inputs
    message: str = "The Version version after handling for the version2 command has not been implemented yet."

    return ExecutionResult(plugin_name, "version2", ExecutionStatus.GENERAL_FAILURE, [message])
    