import:
  files:
    - ./DataMessage.yaml
---
schema:
  name: DataB
  package: flow
  extends:
    - package: flow
      name: DataMessage
  fields:
    - name: transformed_request
      type: string
