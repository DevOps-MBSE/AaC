"""AaC Plugin implementation module for the {{ plugin.name }} plugin."""
# NOTE: It is safe to edit this file.  
# This file is only initially generated by the aac gen-plugin, and it won't be generated if the file already exists.

plugin_version = "0.0.0"

{% for command in commands -%}
{% set comma_joiner = joiner(",") %}
def {{ command.name | replace("-", "_") }}({% for command_input in command.input -%}{{ comma_joiner() }}{{ command_input.name }}: {{ command_input.type }}{%- endfor %}):
    """
    {% if command.description -%}
    {{command.description}}
    {%- else -%}
    TODO: Write a description
    {%- endif %}
    {%- if command.input is defined -%}
    
    {% for command_input in command.input %}
    :param {{command_input.name}} #TODO add a helpful parameter description
    :type {{command_input.type}}
    {%- endfor %}
    {%- endif %}
    """

    # TODO add implementation here NotImplementedError
    raise NotImplementedError()


{%- endfor %}

