[tool.black]
line-length = 99
target-version = ['py39']

[tool.nose2.coverage]
always-on = true

[build-system]
requires = [
    "setuptools>=60",
    "wheel"
]
build-backend = "setuptools.build_meta"
