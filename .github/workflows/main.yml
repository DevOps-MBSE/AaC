name: Main branch AaC Workflow

on:
  push:
    branches: [main]

jobs:
  Build_Package_and_Artifacts:
    uses: jondavid-black/AaC/.github/workflows/build-job.yml@5811933d59552ab65ebf962c3a5cbf3b9ec8e7fa

  Package_Functional_Tests:
    needs: Build_Package_and_Artifacts
    uses: jondavid-black/AaC/.github/workflows/functional-tests.yml@


  # deploy:
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Publish distribution to PyPI
  #     if: startsWith(github.ref, 'refs/tags')
  #     uses: pypa/gh-action-pypi-publish@master
  #     with:
  #       password: ${{ secrets.PYPI_API_TOKEN }}
