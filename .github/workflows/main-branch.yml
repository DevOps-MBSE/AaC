# This workflow is only intended to run when a pull-request is merged into the main branch
name: Main branch AaC Workflow

on:
  push:
    branches: [main]

defaults:
  run:
    working-directory: ./python

jobs:
  Build_Python_Artifacts:
    uses: jondavid-black/AaC/.github/workflows/python/build-artifacts.yml@a612abe7becce08d0543dc0498e95fe7febc89e9

  Python_Functional_Tests:
    needs: Build_Artifacts
    uses: jondavid-black/AaC/.github/workflows/python/functional-tests.yml@a612abe7becce08d0543dc0498e95fe7febc89e9
